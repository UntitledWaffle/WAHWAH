const contentDiv = document.getElementById('content');

if (!contentDiv.classList.contains('highlighted')) {
    contentDiv.classList.add('highlighted');
} else {
    contentDiv.classList.remove('highlighted');
}

// What will be the background color of the "content" div after the JavaScript code executes?