const list = document.getElementById('fruit-list');
const newItem = document.createElement('li');
newItem.textContent = "Cherry";
list.append(newItem);

// How many fruits will be displayed in the list after the JavaScript code runs,
// and what will be the last fruit?